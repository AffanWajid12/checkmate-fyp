# Test Database connecting to our local docker container
DATABASE_URL="postgresql://postgres:postgrespassword@localhost:5433/test_db?schema=public"

# Mock Supabase URL just in case, though we will mock the client directly
SUPABASE_URL="https://mock_supabase_for_testing.co"
SUPABASE_SERVER_KEY="mock_key"
