Do the following steps:
1. cd backend
2. mkdir controllers models routes middleware utils config

For iniitalizing prisma for database interaction, follow these steps:
1.  Install dependencies:
    npm install

3.  Generate Prisma client: (for intellisense)
    npx prisma generate